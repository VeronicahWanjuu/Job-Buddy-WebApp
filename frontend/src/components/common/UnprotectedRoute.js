/**
 * Unprotected Route Component
 * 
 * This component allows routes to be accessed without authentication.
 */

import React from 'react';

const UnprotectedRoute = ({ children }) => {
  return children;
};

export default UnprotectedRoute;
